services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quick-quiz-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
